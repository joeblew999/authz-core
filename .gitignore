# Rust build artifacts
/target/
Cargo.lock

# Cloudflare Workers — local state and build artifacts
.wrangler/
crates/authz-worker/build/

# remy-sport-biz seed data (sibling private repo). Refresh via:
#   mise run biz:upstream:fetch
# Don't commit — keeps private biz schema out of this repo.
vendor/remy-sport-biz/

# Editor / IDE
.vscode/
.idea/
*.iml
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Environment / secrets
.env
.env.*
*.pem
*.key

# Test and benchmark output
/flamegraph.svg
/perf.data
/perf.data.old

# Coverage
/lcov.info
/coverage/
tarpaulin-report.html
tarpaulin-report.json

# Generated docs (local build)
/doc/
